<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <?php 
        $a = 1;
        $b = 1;
        $c = "leo";
        $d = "leo";

        //igual a: ==
        $a == $b;  //true
        
        //Indentico a: ===
        $a === $b; //False

        //Diferente de: != ou <>
        $a != $c; //True

        //Menor que: <
        $a < $b; //False

        //Maior que: >
        $a > $b; //False

        // Menor ou igual: <=
        $a <= $b; //True

        //Maior ou igual: >=
        $a >= $b; //True


    ?>
</body>
</html>